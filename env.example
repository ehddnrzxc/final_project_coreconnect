# ============================================
# CoreConnect 프로젝트 환경 변수 설정 예시
# ============================================
# 이 파일을 복사하여 .env 파일을 만들고 실제 값을 입력하세요.
# docker-compose.yml에서 이 환경 변수들을 사용합니다.

# ============================================
# 데이터베이스 설정
# ============================================
# MySQL 호스트명 (컨테이너 이름 또는 IP 주소)
# 예: mysql-container, localhost, 172.18.0.2 등
MYSQL_HOST=mysql-container
# MySQL 포트 (기본값: 3306)
MYSQL_PORT=3306
# MySQL 사용자명
MYSQL_USER=root
# MySQL 비밀번호
MYSQL_PASSWORD=your_mysql_password

# ============================================
# JWT 설정
# ============================================
JWT_SECRET_KEY=your_jwt_secret_key_here_make_it_long_and_random

# ============================================
# CORS 및 WebSocket 허용 Origin 설정
# ============================================
# 개발 환경 예시:
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# WEBSOCKET_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# 배포 환경 예시 (실제 도메인/IP로 변경):
# CORS_ALLOWED_ORIGINS=http://13.125.225.211,https://yourdomain.com
# WEBSOCKET_ALLOWED_ORIGINS=http://13.125.225.211,https://yourdomain.com

# 여러 Origin을 허용하려면 쉼표로 구분:
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://13.125.225.211
WEBSOCKET_ALLOWED_ORIGINS=http://localhost:5173,http://13.125.225.211

# ============================================
# AWS S3 설정
# ============================================
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=ap-northeast-2
AWS_BUCKET_NAME=your_bucket_name

# ============================================
# SendGrid 메일 서버 설정
# ============================================
SENDGRID_API_KEY=your_sendgrid_api_key

# ============================================
# 프론트엔드 빌드 타임 환경 변수 (선택사항)
# ============================================
# 배포 환경에서는 nginx가 프록시하므로 일반적으로 설정하지 않아도 됩니다.
# 개발 환경에서 직접 백엔드에 연결할 때만 필요합니다.
# VITE_API_BASE_URL=http://localhost:8080/api/v1/
# VITE_WS_BASE_URL=ws://localhost:8080

# ============================================
# 참고사항
# ============================================
# 1. CORS_ALLOWED_ORIGINS와 WEBSOCKET_ALLOWED_ORIGINS는 
#    실제 배포 환경의 도메인/IP로 변경해야 합니다.
# 2. 프론트엔드 환경 변수(VITE_*)는 빌드 타임에 주입되므로,
#    배포 후 변경하려면 이미지를 다시 빌드해야 합니다.
# 3. .env 파일은 절대 Git에 커밋하지 마세요! (.gitignore에 포함되어 있어야 합니다)

