<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goodee.coreconnect.user.repository.UserMapper">
    <select id="findById" parameterType="long" resultType="com.goodee.coreconnect.user.entity.User">
        SELECT * FROM user WHERE user_id = #{id}
    </select>
</mapper>